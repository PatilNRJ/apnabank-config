server:
  port: 9090

spring:
  application:
    name: "cards"
  datasource:
    url: jdbc:mysql://localhost:3306/cards
    username: cards
    password: Krishiv@091221
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect